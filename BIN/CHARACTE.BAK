#include<stdio.h>
#include<conio.h>
#include<ctype.h>
void main()   //if(ch>=65&&ch<=90||ch>=97&&ch<=122)
{
 clrscr();           //if((ch>=65&&ch<=90)||(ch>=97&&ch<=122))
 char ch;
 printf("enter character\n");   //cout<<"  \n";
 scanf("%c",&ch); //cin.get(ch); //cin>>ch;//ch=getchar();//ch=cin.get();
 if(isalpha(ch)) //if((ch>='A'&&ch<='Z')||(ch>='a'&&ch<='z'))
 printf("it's alphabet chaeracter"); //cout<<" ";
 else if(isdigit(ch))  //if(ch>=48&&ch<=57) //if(ch>='0'&&ch<='9')
 printf("it's digit chaacter");
 else
 if(ch==' ')  //if(ch==32)
 printf("it's space character");
 else if(ch=='\n')  //if(ch==10)
 printf("it's new line chaacter");
 else
 printf("other special chacter");
}