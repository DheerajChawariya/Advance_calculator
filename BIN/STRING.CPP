#include<stdio.h>
#include<conio.h>
void main()
{
 clrscr();
 char str[50];
 printf("enter mobile number\n");
 gets(&str[0]); //gets(str);
 //scanf("%s",&str[0]); //scanf("%s",&str[0]);
 puts(&str[0]);//puts(str);
// printf("%s",&str[0]); //printf("%s",str);
}

